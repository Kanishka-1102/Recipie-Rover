services:
  - type: web
    name: recipe-rover
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn src.app:app
    envVars:
      - key: PORT
        value: 8000
